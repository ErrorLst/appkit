# 白名单式忽略：默认忽略一切，只显式放行需要入库的内容。
# 好处：构建产物、缓存、临时文件天然被排除，不必逐个列举。
#
# 关键：!*/ 让 git 能进入所有目录，因此必须在这之后显式排除 build/ 之类，
#       否则 build/ 里的 .cpp/.h（例如 CMake 的编译器探针文件）会被 !*.cpp 放行进来。

*
!*/

# ---- 仓库级文件 ----
!.gitignore
!README.md
!CMakeLists.txt

# ---- 源码 ----
!*.h
!*.hpp
!*.cpp
!*.cc

# ---- 数据：示例配置与测试夹具 ----
!*.ini

# ---- 文档 ----
!doc/*.md

# ---- 工具链配置 ----
!.clangd
!.clang-format
# 注意：.vscode/ 是编辑器本地配置，不入库（原来那条 !.vscode/settings.json 放行规则已移除）

# ---- 覆盖上面的放行规则：构建产物与缓存一律不要 ----
build/
build-*/
out/
.cache/
